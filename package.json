{
  "private": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "lerna": "^2.8.0"
  },
  "scripts": {
    "publish": "lerna publish",
    "updated": "lerna updated",
    "build": "lerna run build",
    "clean": "lerna clean"
  }
}
