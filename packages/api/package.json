{
  "name": "@promaster/api",
  "version": "1.0.3",
  "description": "Promaster API",
  "repository": "https://gitlab.divid.se/promaster-sdk/packages/promaster-api",
  "main": "lib-cjs/index.js",
  "module": "lib-esm/index.js",
  "types": "lib-cjs/index",
  "license": "UNLICENSED",
  "author": "Divid Promaster AB",
  "publishConfig": {
    "registry": "https://npm.divid.se"
  },
  "dependencies": {
    "@promaster/property": "^1.0.3",
    "@promaster/uom": "^1.0.0",
    "isomorphic-fetch": "^2.2.1",
    "ramda": "^0.22.1",
    "ts-exhaustive-check": "^1.0.0"
  },
  "devDependencies": {
    "@types/isomorphic-fetch": "^0.0.34",
    "@types/ramda": "^0.25.29"
  },
  "scripts": {
    "lint": "tslint -t msbuild './{src,test}/**/*.ts{,x}'",
    "build": "yarn build:clean && yarn build:cjs && yarn build:esm",
    "build:clean": "rm -rf ./lib-cjs ./lib-esm",
    "build:cjs": "tsc",
    "build:esm": "tsc --module es2015 --outDir ./lib-esm",
    "test": "rm -rf test/lib && mocha --colors",
    "test:build": "cd test && tsc"
  }
}
