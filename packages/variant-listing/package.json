{
  "name": "@promaster/variant-listing",
  "version": "1.0.6",
  "repository": "https://gitlab.divid.se/promaster-sdk/packages/variant-listing",
  "description": "Generate list of valid variants",
  "main": "lib-cjs/index.js",
  "module": "lib-esm/index.js",
  "types": "lib-cjs/index",
  "license": "UNLICENSED",
  "author": "Divid Promaster AB",
  "publishConfig": {
    "registry": "https://npm.divid.se"
  },
  "dependencies": {
    "@promaster/property": "^1.1.1",
    "ramda": "^0.22.1"
  },
  "devDependencies": {
    "@types/ramda": "^0.25.29"
  },
  "scripts": {
    "build": "yarn build:clean && yarn build:cjs && yarn build:esm",
    "build:clean": "rm -rf ./lib-cjs ./lib-esm",
    "build:cjs": "tsc",
    "build:esm": "tsc --module es2015 --outDir ./lib-esm",
    "test": "rm -rf test/lib && mocha --colors",
    "test:build": "cd test && tsc"
  }
}
